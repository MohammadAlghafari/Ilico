class FileTypeImageOrVideos {
  int? type; // 1 is image / 2 is video
  String? imageFile;
  String? imageType;
  FileTypeImageOrVideos({required this.type, required this.imageFile, required this.imageType});
}
